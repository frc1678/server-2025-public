# Contains information about this schema file
schema_file:
  # Version of schema file
  # Incremented every merged schema change
  version: 1

data:
  match_number:
    type: int
  team_number:
    type: str

--datapoints:
  total_piece_range: tele_total_pieces
  auto_total_piece_range: auto_total_pieces
  tele_total_piece_range: tele_total_pieces
  climb_fail_range: cage_fail